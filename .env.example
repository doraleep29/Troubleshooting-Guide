# The real deployed URL of the ops workspace's troubleshooting-guide page.
# Set this in the Vercel project's Environment Variables — it's inlined at
# build time (NEXT_PUBLIC_*), so changing it requires a redeploy.
NEXT_PUBLIC_GUIDE_URL="https://<your-carbinox-workspace-deployment>.vercel.app/support/troubleshoot"
